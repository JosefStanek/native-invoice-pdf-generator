import ScreenWrapper from "../components/ui/ScreenWrapper";
import LanguageSettings from "../components/SettingsScreen/LanguageSettings";

const SettingsScreen: React.FC = () => {
  return (
    <ScreenWrapper>
      <LanguageSettings />
    </ScreenWrapper>
  );
};

export default SettingsScreen;
